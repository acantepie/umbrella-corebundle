<th {{ attr | html_attributes }}>
    {%- if label is not empty %}
        {%- if translation_domain %}
            {{- (label_prefix ~ label) | trans([], translation_domain) | raw -}}
        {% else -%}
            {{- (label_prefix ~ label) | raw -}}
        {% endif -%}
    {% endif -%}
</th>